version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd todo-app
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: todo-app/.next
    files:
      - '**/*'
  cache:
    paths:
      - todo-app/node_modules/**/*
